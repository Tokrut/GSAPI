"""
Демонстрация улучшенной панели быстрых действий
"""

import streamlit as st
from ui.sidebar_components import display_quick_actions, display_quick_actions_compact
from config import setup_page_config, apply_custom_styles

def demo_quick_actions():
    """Демонстрация панели быстрых действий"""
    
    # Настройка страницы
    setup_page_config()
    apply_custom_styles()
    
    # Заголовок
    st.title("🎨 Демонстрация улучшенной панели быстрых действий")
    
    st.markdown("""
    ## ✨ Что нового в панели быстрых действий:
    
    1. **Современный дизайн** - градиентный фон с анимацией
    2. **Красивые кнопки** - с hover-эффектами и иконками
    3. **Модальное окно подтверждения** - для критических действий
    4. **Анимации** - плавное появление элементов
    5. **Адаптивный дизайн** - для разных размеров экрана
    6. **Подсказки** - встроенная справочная информация
    """)
    
    # Имитация состояния пользователя для демонстрации
    if 'user' not in st.session_state:
        st.session_state.user = None
    if 'analysis_history' not in st.session_state:
        st.session_state.analysis_history = []
    
    # Основная панель быстрых действий
    st.markdown("### 🚀 Полная версия панели")
    display_quick_actions()
    
    st.markdown("---")
    
    # Компактная версия
    st.markdown("### 📱 Компактная версия")
    display_quick_actions_compact()
    
    st.markdown("---")
    
    # Демонстрация функций
    st.markdown("### 🎭 Демонстрация функций")
    
    col1, col2, col3 = st.columns(3)
    
    with col1:
        if st.button("🏠 Главное меню (демо)"):
            st.success("Переход в главное меню")
    
    with col2:
        if st.button("🔍 Новый анализ (демо)"):
            st.info("Запуск нового анализа")
    
    with col3:
        if st.button("📊 Статистика (демо)"):
            st.warning("Показ статистики")
    
    col1, col2 = st.columns(2)
    
    with col1:
        if st.button("📄 Пример отчета (демо)"):
            st.info("Показ примера отчета")
    
    with col2:
        if st.button("🗑️ Очистить (демо)"):
            st.error("Очистка истории (требует подтверждения)")

if __name__ == "__main__":
    demo_quick_actions()